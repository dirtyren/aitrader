# Pending hypotheses for the RSI Reversion strategy
# Add any deferred single-variable optimization hypotheses here

# ✅ APPLIED 2026-05-25 — Changed to RSI(5) < 35 on 1H for 15 best symbols (see full sweep backtest).

# Alternative: Reduce stop_loss_pct from 2.0 to 1.5
# Rationale: With RSI(5) on 1H, moves are smaller and tighter stops reduce drawdown. Try if fills improve but DD exceeds 8%.

# Alternative: Re-test on 5Min after period change stabilizes
# Rationale: RSI(2) < 40 on 5Min had the highest signal count and broadest coverage. Could apply period=2 with threshold=40 instead.

# Alternative: Increase position_size_r from 0.5 to 1.0
# Rationale: With higher win rate (~80%), sizing up could lift total PnL faster. Only after verifying DD is under control.
